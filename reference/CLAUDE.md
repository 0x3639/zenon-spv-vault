# reference/

`reference/go-zenon/` is a git submodule. **Read-only** — never edit files inside it.

**Pinned commit:** `667a69d9e9a418edf7580b08492ba5dcb9efd63a` (master, as of 2026-04-28)

To update the pin, run `make submodule-update` (added in Phase 4).

Real navigation content (key packages, how to use `go doc`/`gopls`, Serena tips) lands in Phase 3.
